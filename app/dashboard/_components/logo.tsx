export const Logo = () => {
    return (
        // eslint-disable-next-line @next/next/no-img-element
        <img
            height={100}
            width={100}
            alt="logo"
            src="/logo.png"
        />
    )
}